services:
  # rest-api-service:
  #   image: hazardsoft/rest-api:latest
  #   command: sh -c "npx prisma generate && npm run start:dev"
  #   depends_on:
  #     - database-service
  #   ports:
  #     - ${PORT}:${PORT}
  #   working_dir: /app
  #   volumes:
  #     - ./:/app
  #   environment:
  #     - DATABASE_URL=${DATABASE_URL}
  database-service:
    image: hazardsoft/rest-database:latest
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - PGDATA=${PGDATA}
    volumes:
      - ./prisma/migrations/${PRISMA_MIGRATION}:/docker-entrypoint-initdb.d
      - pg-data:${PGDATA}

volumes:
  pg-data:
